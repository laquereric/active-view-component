<!-- Title: < %= props[:title] %> -->
<body>
  Body<br/>
  
  <!-- class="< % = @component.style.class %>" -->
  <!-- < %= data_attributes.map { |key, value| "data-#{key}=\"#{value}\"" }.join(' ').html_safe -->
  <!-- < %= @content %> -->
</body>
