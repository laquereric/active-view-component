<!--  lang="< % = @style.lang %>" dir="< % = @style.dir %>" < % = @style.html_attributes.map { |key, value| "#{key}=\"#{value}\"" }.join(' ').html_safe %>> -->
<html>

    <%= 
      render(self.children[0])
    %>
    <br/>
    <%= 
      render(self.children[1])
    %>

<!--
  < % = 
    render( @component.with_body_content ) do |body_component|
      body_component.props = body_props
    end
  % >
  -->

</html>
